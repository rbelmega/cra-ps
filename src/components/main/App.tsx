import React from 'react';
import Header from '../header/Header';
import Body from '../body';

export const App = () => (
  <div className="main-app">
    <Header />
    <Body />
  </div>
);

export default App;
